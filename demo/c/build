#!/bin/bash
Fail()
{
    echo "FATAL($0): $1"
    exit 1
}

for name in moonphase positions riseset seasons; do
    gcc -Wall -Werror -o ${name} -I../../source/c ../../source/c/astronomy.c astro_demo_common.c ${name}.c -lm ||
        Fail "Error building ${name}.c"
done

exit 0
